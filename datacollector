class bot:
    def __init__(self):

    def scrape():

        //

        x()
        searchbar()
        sitetyper1()

        while
        if data1 == true(
            list.append.matches() // send data to a place to store
        ) else (
            scrolldown() // forever until end of page
        )
        if 
        if 1 + pages == true(
            keystroke.navnextpage()
        )   else ??
        return

        data is sent to array and then...

        //array/list/matches

        array should be sorted? and then 

        figure out what kind of number specifically and categorize

        look for accesory data? to help match
        //nearby names or other numbers

        continue searching thru list pof websites for information and then send 
        certain data to certain places, what can be done withthis information?

        we can combine it with something else/// what else.
